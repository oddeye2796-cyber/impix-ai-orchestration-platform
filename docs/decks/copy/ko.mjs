// Korean is the source language: the generators' own string literals are the
// copy, so this catalogue is empty and tr() passes everything through.
export const copy = {};

export const introFile = 'IMPIX_AI_플랫폼_소개서';
export const manualFile = 'IMPIX_AI_사용_매뉴얼';
